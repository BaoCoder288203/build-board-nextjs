# BuildBoard Web (Next.js)

Bootstrapped with `create-next-app` (npm).

## Requirements

```bash
nvm use 20.19.4
```

## Setup

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

Env: `.env.local` (local `NEXT_PUBLIC_*` only for Phase 0).
